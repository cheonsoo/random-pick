<script>
  import Chip from './Chips.svelte';

  export let candidates;
  export let addItem = null;
  export let delItem = null;
</script>

<div class="candidates">
  <Chip keyed={candidates.map((item, idx) => ({ k: idx, v: item }))} />
  <!-- {#each candidates as item}
    <div class="tag">
      {item}
      <div></div>
    </div>
  {/each} -->

  <input type="button" on:click={addItem} />
</div>

<style>
.candidates {
  width: 100%;
  height: 200px;
  position: fixed;
  top: 0;
  left: 0;
  z-index: 11;
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
}

.tag {
  height: 30px;
  padding: 10px;
  margin: 10px;
  border-radius: 10px;
  background: green;
  font-weight: 900;
}
</style>